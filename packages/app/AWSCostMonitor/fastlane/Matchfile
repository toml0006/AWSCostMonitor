# Fastlane Match Configuration

# Git repository for storing certificates and profiles
git_url("https://github.com/toml0006/certificates")

# Storage mode - git is the default
storage_mode("git")

# Type of certificates/profiles to generate
type("appstore") # or "development", "adhoc", "enterprise"

# App identifier(s) to generate certificates for
app_identifier("middleout.AWSCostMonitor")

# Your Apple Developer Team ID
team_id("TJSYWP4C3D")

# Username for Apple Developer Portal
username("jackson.tomlinson@gmail.com")

# Platform
platform("macos")

# Clone the certificates repo to a specific location (optional)
# clone_branch_directly(true)

# Generate Mac App Store profiles
generate_apple_certs(true)

# Readonly mode - set to true in CI environments
readonly(false)

# Verbose output
verbose(true)